import type { Meta, StoryObj } from '@storybook/react';
const meta: Meta = { title: 'Operator/ProductionRegistrationModal' };
export default meta;
export const Default: StoryObj = { args: { isOpen: true } };
