import type { Meta, StoryObj } from '@storybook/react';
const meta: Meta = { title: 'Operators/MachineAssignmentModal' };
export default meta;
export const Default: StoryObj = { args: { isOpen: true } };
