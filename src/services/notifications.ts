export const notificationsService = {
  send: async (userId: string, message: string) => ({ success: true }),
  subscribe: async (userId: string) => ({ success: true }),
  unsubscribe: async (userId: string) => ({ success: true }),
};
