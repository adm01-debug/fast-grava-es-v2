import { describe, it, expect } from 'vitest';
import { reportWebVitals } from './webVitals';

describe('webVitals', () => {
  it('reportWebVitals should be defined', () => { expect(reportWebVitals).toBeDefined(); });
});
